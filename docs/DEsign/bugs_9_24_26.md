<!--
  RESTORED 2026-09-26. This list was rewritten from an 11-item snapshot while it
  had grown to 34, and the other 23 items were overwritten on disk. They are
  restored below from the session transcript; git never held a copy.
  Untouched verbatim recovery: bugs_9_24_26_RECOVERED.md
-->

- [x] ~~PLayer should start with no default armor, just their clothes~~ — removed the starting `PADDED_ARMOR`; the chest slot now begins empty, so the first armour found is a real upgrade.
- [x] ~~Let's increase the height of the starting shelter's ceiling by 50%~~ — shelter tiles now build at `SHELTER_CEILING_Y = 1.5` vs `STANDARD_CEILING_Y = 1.0`. Walls rise with the ceiling and the wall texture **tiles** into the extra height rather than stretching. Window embrasures and sliding door panels follow the same per-tile height; both had `1.0` baked in, which left a half-unit hole to the sky above the shelter's own door.
- [x] ~~Decrease the spawn rate of the merchant by 50%~~ — the merchant had **no spawn roll at all**: it spawned on every eligible level 1–2 chunk, which on a 441-chunk maze meant one almost everywhere. Now one per level, via `ShopkeeperTracker`.
- [x] ~~Blood splatter should never appear in an open doorway as it looks like the blood is floating in the air~~ — two separate defects. Decals kept their spawn height (kill stains are born at chest height, y=0.5) and the raycaster draws from that value, so they hung at eye level; now clamped to the floor. And `spawnSurfaceDecal` took no `Maze`, so no caller could reject a doorway. `GoreManager` now holds the current maze and screens **every** spawn path through one guard — an earlier version added an opt-in overload, which left the combat-blood and kill-stain sites (the reported case) still unguarded. It fails open on coordinates it cannot resolve.
- [x] ~~While play testing, I heard the 'death' sound when I traveled down a ladder, this was not supposed to happen.~~ — the ladder played `sounds/music/tarmin_enter_fx.ogg`, which **is** the retired player-death asset. Commit `442c15d` rewired the death *key* but left call sites referencing the old file by raw path. Now a registered `ladder_transition` key.
- [x] ~~After dying and restarting in the shelter, sometimes the flaming red sky is no longer visible and the sky is completely dark despite the time of day.~~ — `GameScreen` kept its own `currentLevel` alongside `WorldManager`'s, and `respawnInShelter` never reset it. Dying below the surface left it deep, making `canRender3DSky` false so the skybox was **never drawn** — the frame cleared to near-black. The duplicate field is gone; depth now has one source of truth.
- [x] ~~Opening a bag container makes the noies of opening a chest, we need to ensure the sounds we are using are for the correct items.~~ — the handler branched on `ItemCategory.CONTAINER`, so every container shared the chest's lid-and-latch. Now branches on `ItemType`: bags, packs, the bag of holding and the money belt get a new `bag_open` cue; chests and boxes keep `chest_open`.
- [x] ~~The unlock rate for items is far too aggressive, let's reduce the curve~~ — grant capped at 2 per run (was 3) **and** milestones raised to 100 kills / 150 divinities / 1000 turns (were 25 / 50 / 300). Those old bars were reachable in an ordinary run, so nearly every run hit the cap.
- [x] ~~The amount of items available to a new game must be VERY limited, making unlocks and replays more enjoyable.~~ — `UNLOCK_SCORE_THRESHOLD` 310 → 50. It sat *above* the median item score of 210, so a brand-new save already saw **379 of 544** templates. Now roughly 134.
- New Feature, when bridge integrity reaches 100%, a boss should spawn on the first chunk that the player must kill in order to reset the integrity level. This will require the player to prepare carefully for this scenario by storing items in their shelter chest (meaning they also need to unlock the chest and as many other shelter amenities as possible)
  - **Spec written, not built**: `docs/DEsign/Requirements_ Bridge Integrity Boss.md`. Deliberately deferred — it is a feature, not a fix. The spec forces the decision the request leaves open (what "reset the integrity level" means numerically) and flags that 100% integrity currently **wipes the save**, so this replaces an apocalypse with a boss fight.
- [x] ~~When the player applies crude pressure to a wound and it DOESN'T resolve the wound, they need to lose health, otherwise they can just keeep retrying until it works.~~ — a failed attempt now costs 2 HP **and** a turn. The HP alone would only have slowed the grind; the turn is what makes it a decision, since monsters never acted while the modal was open.
- I encountered strange red and blue squares on the floor on dungeon level 2 and I don't know why (screenshot C:\workspace\Tarmin2\docs\DEsign\screenshots\blue_red.png)
- We need a spell that can be unlocked which warps the player back to the shelter
- I found an arcane war book that stated it does 0 dmg
- The merchants available inventory should only pull from the pool of available unlocked items
- After casting a spell, the player recovers 1 MP amost immediately, we need to slow the MP recovery rate and gate it to one of the player's attributes so it can improve over time with leveling
- I encounted the 'undead graveyard' themed chunk but the desecrated graves were invisible despite providing collision to the player's movement, they also show up on the mini-map but nothing renders in the field of view.
- Now thatwe are providing an easy way for the player to return to the shelter (via the spell), let's change the penalty for death so that the player loses their equipped armor and weapons upon death. They always start with the rusty sword (1d3)
- Currently I see special moves in toast text during combat (titan's cleave etc). The list of special moves is far too limited and needs to be dramatically expanded to accommodate the various weapon archtypes and combat animations.
- The level curve is a bit too punishing for early leveling. Review recent run telemetry and adjust to make it slightly easier to reach level one but ensure the curve is still balanced.
- When a wound festers into illness, what actually happens? How do you treat illness right now?
- We need a better way to represent water in the 'water logged' themed chunks.
- When the player dies and revives in the shelter, their starting food, hunger and thirst need to reset to original values.
- What happened to the level up system where the player can unlock skills from a tree and assign attributes increase to their stats?
- I have purchased new assets here C:\workspace\Tarmin2\docs\game_assets\bt25_ultimatesfxforgames_softwarebundle, we need to perform a detailed analysis and catalogue of all of the new effects and determine what we want to use in the game. This needs to be documented in a new markdown file that stores the research for future analysis
- I have purchased new assets here C:\workspace\Tarmin2\docs\game_assets\, we need to perform a detailed analysis and catalogue of all of the new effects, sounds and images and determine what we want to use in the game. This needs to be documented in a new markdown file that stores the research for future analysis
- We need to introduce variety in the types of Key's the player finds and only certain key types should unlock certain locked containers, otherwise a single key can unlock all containers for the rest of the game.
- We need to overhaul ALL weapons and their damage, damage types, combat animation and special aspects (bleed, bludgeon / stun, sever, etc) use open5e website to source the info
- When viewing items on the ground in the notification window OR in investory, we should indicate clearly whether or not the item is better then what is currently equipped by the player in the equivalent slot
- Monsters should ALWAYS leave a corpse when killed that persists
- Spells learned by reading random spell books in the field should only apply to that run. Spells learned via unlocking via the leveling system or TOMES should be permanent and remain with the player after death.
- I drank a potion of heroism and the listed effect did not actually happen, what other effects attached to consumables are not implemented??
- A was able to engage in combat with a Zombie through a wall.
- The HUD UX will sometimes get out of alignment (see badUX.png) in screenshots. We need to polish the HUD UX to be stable, responsive and READABLE
- When the player Starts a New Expedition from the main menu, it should wipe all save progress (progression) and all of that save data should only be applicable to that save file, there should be no global progression that exists outside of the context of a save file.
- [x] ~~We need to vary the wall textures used in the maze. For the first phase of this I added additional wall assets to the images folder. The game should randomly choose 1 per wall. I also included a grey variation of the original textures. We should also assign a random 'color theme' to a maze chunk overall. For now, we will have Green and Grey. Wire this up.~~ — per-face variant and per-chunk Green/Grey palette, both derived from the chunk seed so they survive leaving and re-entering and need no save change. MAZE biome only. Required bucketing wall quads by variant (all walls were one mesh with one texture) and replacing RETRO's texture-reference check with a Surface tag, which would otherwise have drawn every new variant as a ceiling. Also fixed the reload path never restoring chunk id, biome or theme — a latent bug that was already costing reloaded forest chunks their cliff walls.

---

## Regression cover

New tests, each checked by mutation (reverting the fix must fail the test):

- `ShelterCeilingHeightTest` — shelter and corridor tiles disagree on ceiling height, and the raised constant really is raised.
- `SurfaceDecalPlacementTest` — no decal lands on a door or gate tile; out-of-range coordinates fail open.
- `ShopkeeperTrackerTest` — one merchant per level, and a fresh run can claim again. The related ordering fix (a chunk with nowhere to stand must not burn the level's only slot) is in `MazeChunkGenerator` and is **not** covered by a test; placement needs an `AssetManager`, so it is reasoned-about rather than proven.
- `ProgressionUnlockOverhaulTest` / `CodexDataAndFilteringTest` — retuned; the cap test now supplies telemetry that actually clears the raised bars (it previously met none of them, so the cap was never exercised) and the strata test is anchored on real scores rather than on the threshold constant.

---

## Not yet addressed

Two things found while working through the list, neither on it:

- [x] ~~**`Weather3DTest` is flaky** (~1 run in 10)~~ — fixed by `09195be`, merged from `feat/volcanic-sky-polish`, which culls splash droplets that drift under a roof. Diagnosis was: `WeatherRenderer.spawnSplash` checks the *spawn* tile is outdoors, then gives droplets outward velocity so they drift onto home tiles. Nothing wrong is ever drawn — the render path filters indoors — the test just inspects `getSplashDroplets()` rather than what is rendered.
- [x] ~~**Duplicate item templates**: both `SHIELD_LARGE`/`LARGE_SHIELD` and `SHIELD_SMALL`/`SMALL_SHIELD` exist as separate entries. That duplication is how their colour ladders drifted apart and produced the `COPPER` crash.~~ — duplicate **data** removed from `armor.json` and the drop table (both spellings were spawning, so the same shield rolled twice); canonical `SMALL_SHIELD`/`LARGE_SHIELD` keep the fuller ladder, so large shields retain their BLUE mid tier. The duplicate **enum names** are deliberately kept and aliased at load: saves store `ItemType` by name, and an unknown name does not lose one item — it nulls the whole `player.json`, regenerates a chunk, or empties the shelter chest. Also fixed three adjacent bugs: `ShelterChest.load` cleared before parsing (an unreadable file silently emptied the stash, then saved it empty); `SHIELD_ROUND_WOODEN` had art, data and a paperdoll layer but no enum value so it was dropped at load and could never spawn; `SHIELD_MEDIUM` could spawn with no paperdoll layer and equipped invisibly.
- Undead creatures should not spill blood when hit in general.
- To combat / cure blood diseases, fevers, infections, the player should be able to cook 'broth' from monster parts and bones. These cook sessions result in potions of various broth types that can cure the aforementioned illnesses. This requires having unlocked the cooking pot / portable gear
